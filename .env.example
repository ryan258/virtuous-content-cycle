# ================================
# Virtuous Content Cycle - Environment Configuration
# ================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# --------------------------------
# Server Configuration
# --------------------------------
PORT=3000
APP_NAME=Virtuous Content Cycle
APP_BASE_URL=http://localhost:3000

# --------------------------------
# OpenRouter API Configuration
# --------------------------------
# Get your API key from: https://openrouter.ai/keys
# Leave empty or remove to run in mock mode (no API calls, simulated responses)
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# --------------------------------
# AI Model Selection (Updated Nov 2025)
# --------------------------------
# Sherlock Think Alpha: Reasoning model - slower but smarter (RECOMMENDED)
#   - 1.8M context window, excels at tool calling
#   - FREE during alpha testing
#   - Best for thoughtful content feedback & editing
#
# Sherlock Dash Alpha: Speed model - faster but no reasoning
#   - Use if you need speed over quality
#
# Alternative models:
#   - anthropic/claude-3.5-sonnet (paid, excellent quality)
#   - google/gemini-1.5-flash (paid, fast & cheap)
#
# See: https://openrouter.ai/openrouter/sherlock-think-alpha

OPENROUTER_FOCUS_MODEL=openrouter/sherlock-think-alpha
OPENROUTER_EDITOR_MODEL=openrouter/sherlock-think-alpha

# --------------------------------
# Mock Mode (for testing without API)
# --------------------------------
# Set to 'true' to force mock mode even with an API key
# Useful for testing without incurring API costs or during development
# USE_MOCK_AI=false

# --------------------------------
# Setup Instructions
# --------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Add your OpenRouter API key (or leave empty for mock mode)
# 3. Install dependencies: npm install
# 4. Initialize database: node migrate.js
# 5. Start server: npm start (or npm run dev for auto-reload)
#
# Mock Mode: Runs without API calls, generates simulated feedback
# Live Mode: Uses real AI models via OpenRouter API